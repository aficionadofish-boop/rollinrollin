---
phase: 02-monster-import-and-library
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/ui/library_tab.py
  - src/ui/monster_detail.py
  - src/ui/import_log.py
autonomous: false
requirements:
  - IMPORT-01
  - IMPORT-02
  - IMPORT-03
  - IMPORT-04
  - IMPORT-05
  - LIB-02
  - LIB-03
  - LIB-04
  - LIB-05
  - LIB-06

must_haves:
  truths:
    - "User can click Import > File(s) and select one or more .md files; after import the monster list updates to show new monsters"
    - "User can click Import > Folder and select a directory; all .md files in the folder are imported and results appear in the scrollable log panel"
    - "When a duplicate monster name is imported, a dialog appears asking Keep Both / Replace / Skip — no silent overwrite occurs"
    - "The import log panel (not a modal) shows per-file results: filename, count imported, count incomplete, count failed with failure details"
    - "User can type in the search bar and the monster list filters instantly to matching name/CR/type entries"
    - "User can select a type from the Type dropdown and only monsters of that type appear in the list"
    - "User can toggle the Incomplete filter and only monsters with incomplete=True appear in the list"
    - "User can click a column header (Name or CR) and the list sorts by that column; CR sorts numerically"
    - "Clicking a monster in the list shows its full statblock in the detail panel: name, AC, HP, CR, type, ability scores, saves, and all actions"
    - "Actions with is_parsed=True show a Roll button placeholder; actions with is_parsed=False show raw text only with no button"
    - "The lore section in the detail panel is collapsed by default; clicking it expands to show lore text"
    - "User can edit the tag field in the detail panel and the change is saved to the Monster object in the library"
  artifacts:
    - path: "src/ui/library_tab.py"
      provides: "MonsterLibraryTab(QWidget): main tab widget with splitter, toolbar, search/filter controls, import logic"
      exports: ["MonsterLibraryTab"]
      min_lines: 120
    - path: "src/ui/monster_detail.py"
      provides: "MonsterDetailPanel(QWidget): full statblock display with collapsible lore and tag editor"
      exports: ["MonsterDetailPanel"]
      min_lines: 80
    - path: "src/ui/import_log.py"
      provides: "ImportLogPanel(QTextEdit): read-only scrollable log for import results"
      exports: ["ImportLogPanel"]
  key_links:
    - from: "src/ui/library_tab.py"
      to: "src/library/service.py"
      via: "holds shared MonsterLibrary instance; calls library.add/replace/has_name after import"
      pattern: "self\\._library"
    - from: "src/ui/library_tab.py"
      to: "src/parser/statblock_parser.py"
      via: "calls parse_file(path) for each imported file"
      pattern: "parse_file\\("
    - from: "src/ui/library_tab.py"
      to: "src/ui/monster_table.py"
      via: "calls model.reset_monsters(library.all()) after each import batch completes"
      pattern: "reset_monsters\\("
    - from: "src/ui/library_tab.py"
      to: "src/ui/monster_filter.py"
      via: "search bar textChanged -> proxy.setFilterFixedString; type combo -> proxy.set_type_filter; incomplete toggle -> proxy.set_incomplete_only"
      pattern: "proxy\\.setFilterFixedString|set_type_filter|set_incomplete_only"
    - from: "src/ui/library_tab.py"
      to: "src/ui/monster_detail.py"
      via: "view selectionChanged signal → detail_panel.show_monster(monster)"
      pattern: "show_monster\\("
    - from: "src/ui/library_tab.py"
      to: "src/ui/import_log.py"
      via: "import result displayed via log_panel.log_result()"
      pattern: "log_result\\("
---

<objective>
Assemble the complete Monster Library tab: import toolbar with File/Folder submenu, import log panel, search bar, type filter dropdown, incomplete toggle, sortable monster table, and monster detail panel with expandable lore and tag editor. This plan integrates everything from Plans 02 and 03.

Purpose: This is the user-facing integration — all parser and library machinery is in place; this plan wires it into a working UI that a DM can use to import and browse monsters.

Output: Three UI widget files (MonsterLibraryTab, MonsterDetailPanel, ImportLogPanel) that compose into a functional library tab.
</objective>

<execution_context>
@C:/Users/farka/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/farka/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-monster-import-and-library/02-RESEARCH.md
@.planning/phases/02-monster-import-and-library/02-02-SUMMARY.md
@.planning/phases/02-monster-import-and-library/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ImportLogPanel + MonsterDetailPanel</name>
  <files>
    src/ui/import_log.py
    src/ui/monster_detail.py
  </files>
  <action>
    Create src/ui/import_log.py with ImportLogPanel(QTextEdit):
      Imports: PySide6.QtWidgets (QTextEdit); src.parser.models (ParseFailure)

      class ImportLogPanel(QTextEdit):
        __init__(self, parent=None):
          super().__init__(parent)
          self.setReadOnly(True)
          self.setPlaceholderText("Import results will appear here...")
          self.setMinimumHeight(120)

        log(self, message: str) -> None:
          self.append(message); self.ensureCursorVisible()

        log_result(self, filename: str, success: int, incomplete: int, failures: list) -> None:
          """Format one file's import result into the log."""
          self.append(f"--- {filename} ---")
          self.append(f"  Imported: {success}  Incomplete: {incomplete}  Failures: {len(failures)}")
          for f in failures:
            name_part = f" [{f.monster_name}]" if f.monster_name else ""
            self.append(f"  FAIL{name_part}: {f.reason}")
          self.ensureCursorVisible()

        clear_log(self) -> None:
          self.clear()

    Create src/ui/monster_detail.py with MonsterDetailPanel(QWidget):
      Imports: PySide6.QtWidgets (QWidget, QVBoxLayout, QHBoxLayout, QGridLayout,
               QLabel, QPushButton, QLineEdit, QGroupBox, QScrollArea, QSizePolicy);
               PySide6.QtCore (Qt); src.domain.models (Monster, Action)

      ABILITY_LABELS = ['STR', 'DEX', 'CON', 'INT', 'WIS', 'CHA']

      class MonsterDetailPanel(QWidget):
        Layout: QScrollArea wrapping a QWidget with QVBoxLayout

        Sections (top to bottom):
          1. Name label (large, bold)
          2. Stats grid (QGridLayout, 2-col label/value pairs):
             AC | HP | CR | Type
          3. Ability scores row (6-column grid of score+modifier label pairs):
             STR DEX CON INT WIS CHA, each showing "10 (+0)" format
          4. Saving throws label (comma-separated bonuses, or "None" if empty)
          5. Actions section label ("Actions") + action rows:
             For each action:
               - If is_parsed=True: QHBoxLayout with action name label + "Roll" QPushButton
                 (QPushButton.setEnabled(False) for now — Phase 3 will wire it)
               - If is_parsed=False or to_hit_bonus is None: QLabel with raw_text, word-wrap
          6. Tags QLineEdit with label "Tags:" — shows comma-separated monster.tags
             Connect textChanged to update monster.tags on the live Monster object
             (direct mutation acceptable — library owns the Monster instance)
          7. Lore QGroupBox "Lore & Description":
             setCheckable(True); setChecked(False) — collapsed by default
             Contains QLabel(word_wrap=True) showing monster.lore
             If lore is empty, shows "No lore recorded."

        show_monster(self, monster: Monster) -> None:
          Stores self._current_monster = monster
          Updates all labels and widgets to reflect the new monster's data
          Clears and rebuilds the actions section:
            Remove all widgets from actions layout using standard Qt clear pattern:
              while layout.count(): item = layout.takeAt(0); widget = item.widget();
              if widget: widget.deleteLater()
            Rebuild with new monster's actions
          Updates lore group with monster.lore text (or "No lore recorded." if empty)
          Updates tags field with ", ".join(monster.tags) (block textChanged signal during update
          using self._tags_edit.blockSignals(True/False) to avoid spurious tag mutation)

        clear(self) -> None:
          Sets all labels to "" and clears actions section and lore
          Called when no monster is selected

        _modifier_str(score: int) -> str:
          Returns "(+N)" or "(-N)" format: modifier = (score - 10) // 2
          Note: use Python integer division which rounds toward negative infinity;
          this is correct for D&D 5e ability modifiers (not int()/truncation toward zero)

        _on_tags_changed(self, text: str) -> None:
          If self._current_monster is not None:
            tags = [t.strip() for t in text.split(",") if t.strip()]
            self._current_monster.tags = tags
  </action>
  <verify>
    python -c "from src.ui.import_log import ImportLogPanel; from src.ui.monster_detail import MonsterDetailPanel; print('OK')"
    Imports must succeed without error (no Qt instantiation needed for import check)
  </verify>
  <done>
    - ImportLogPanel importable; log_result() formats multi-line entry correctly
    - MonsterDetailPanel importable; show_monster() method exists with correct signature
    - Roll button present for parsed actions, absent for unparsed actions
    - Lore QGroupBox uses setCheckable(True)/setChecked(False) pattern
    - Tags QLineEdit connected to monster.tags mutation via blockSignals guard
  </done>
</task>

<task type="auto">
  <name>Task 2: MonsterLibraryTab (Full Integration)</name>
  <files>
    src/ui/library_tab.py
  </files>
  <action>
    Create src/ui/library_tab.py with MonsterLibraryTab(QWidget):

    Imports:
      from pathlib import Path
      from PySide6.QtWidgets import (QWidget, QVBoxLayout, QHBoxLayout, QSplitter,
        QTableView, QLineEdit, QComboBox, QPushButton, QMenu, QLabel, QFileDialog,
        QMessageBox, QHeaderView, QAbstractItemView)
      from PySide6.QtCore import Qt, QItemSelectionModel
      from src.library.service import MonsterLibrary
      from src.parser.statblock_parser import parse_file
      from src.parser.models import ImportResult
      from src.ui.monster_table import MonsterTableModel
      from src.ui.monster_filter import MonsterFilterProxyModel
      from src.ui.monster_detail import MonsterDetailPanel
      from src.ui.import_log import ImportLogPanel

    class MonsterLibraryTab(QWidget):
      __init__(self, library: MonsterLibrary, parent=None):
        super().__init__(parent)
        self._library = library
        self._setup_ui()
        self._connect_signals()

      _setup_ui(self):
        Main layout: QVBoxLayout

        Toolbar row (QHBoxLayout):
          - Import QPushButton ("Import…") with a QMenu:
              Action "File(s)…" → calls _import_files()
              Action "Folder…" → calls _import_folder()
            Attach menu: import_btn.setMenu(import_menu)
          - QLabel("Search:")
          - self._search_edit = QLineEdit() with placeholderText("Search name, type, CR…")
          - QLabel("Type:")
          - self._type_combo = QComboBox(); addItem("All Types") as first item
          - self._incomplete_check = QComboBox(); addItems(["All", "Incomplete only", "Complete only"])
          - addStretch() at end

        Left panel (monster list):
          self._model = MonsterTableModel(parent=self)
          self._proxy = MonsterFilterProxyModel(parent=self)
          self._proxy.setSourceModel(self._model)
          self._proxy.setSortRole(Qt.UserRole)
          self._table = QTableView()
          self._table.setModel(self._proxy)
          self._table.setSortingEnabled(True)
          self._table.setSelectionBehavior(QAbstractItemView.SelectionBehavior.SelectRows)
          self._table.setSelectionMode(QAbstractItemView.SelectionMode.SingleSelection)
          self._table.horizontalHeader().setSortIndicatorShown(True)
          self._table.horizontalHeader().setStretchLastSection(False)
          self._table.horizontalHeader().setSectionResizeMode(0, QHeaderView.ResizeMode.Stretch)
          self._table.setEditTriggers(QAbstractItemView.EditTrigger.NoEditTriggers)
          self._table.verticalHeader().setVisible(False)

        Right panel (detail + log):
          QVBoxLayout with:
            self._detail_panel = MonsterDetailPanel(parent=self)
            self._log_panel = ImportLogPanel(parent=self)
            self._log_panel.setMaximumHeight(180)

        QSplitter (horizontal):
          addWidget(left_widget containing table)
          addWidget(right_widget containing detail + log)
          setStretchFactor(0, 2); setStretchFactor(1, 3)

        Add toolbar row and splitter to main layout.

      _connect_signals(self):
        self._search_edit.textChanged.connect(self._proxy.setFilterFixedString)
        self._type_combo.currentTextChanged.connect(self._on_type_filter_changed)
        self._incomplete_check.currentIndexChanged.connect(self._on_incomplete_filter_changed)
        self._table.selectionModel().selectionChanged.connect(self._on_selection_changed)

      _on_type_filter_changed(self, text: str):
        if text == "All Types":
          self._proxy.set_type_filter("")
        else:
          self._proxy.set_type_filter(text)

      _on_incomplete_filter_changed(self, index: int):
        if index == 0:    # All
          self._proxy.set_incomplete_only(False)
          self._proxy.set_type_filter(self._proxy._type_filter)  # preserve type filter
        elif index == 1:  # Incomplete only
          self._proxy.set_incomplete_only(True)
        elif index == 2:  # Complete only — add set_complete_only method to proxy, or handle inline
          self._proxy.set_incomplete_only(False)
          # Note: "complete only" requires an additional filter flag; add set_complete_only(bool)
          # to MonsterFilterProxyModel and call it here. This is the simplest extension to the
          # proxy's filterAcceptsRow: if self._complete_only and monster.incomplete: return False

      _on_selection_changed(self, selected, deselected):
        indexes = selected.indexes()
        if not indexes:
          self._detail_panel.clear()
          return
        proxy_index = indexes[0]
        source_index = self._proxy.mapToSource(proxy_index)
        monster = self._model.monster_at(source_index.row())
        self._detail_panel.show_monster(monster)

      _import_files(self):
        files, _ = QFileDialog.getOpenFileNames(
          self, "Import Monster Files", "", "Markdown Files (*.md);;All Files (*)"
        )
        if not files: return
        for file_path in files:
          self._process_import_file(Path(file_path))
        self._refresh_model()
        self._refresh_type_combo()

      _import_folder(self):
        folder = QFileDialog.getExistingDirectory(
          self, "Import Monsters from Folder", "",
          QFileDialog.Option.ShowDirsOnly
        )
        if not folder: return
        md_files = list(Path(folder).glob("*.md"))
        if not md_files:
          self._log_panel.log(f"No .md files found in {Path(folder).name}/")
          return
        for file_path in md_files:
          self._process_import_file(file_path)
        self._refresh_model()
        self._refresh_type_combo()

      _process_import_file(self, path: Path):
        result = parse_file(path)
        for monster in result.monsters:
          if self._library.has_name(monster.name):
            action = self._ask_duplicate_action(monster.name)
            if action == 'skip':
              continue
            elif action == 'replace':
              self._library.replace(monster)
            else:  # 'keep'
              self._library.add(monster)
          else:
            self._library.add(monster)
        import_result = ImportResult.from_parse_result(str(path.name), result)
        self._log_panel.log_result(
          import_result.filename,
          import_result.success_count,
          import_result.incomplete_count,
          import_result.failures,
        )
        for warning in result.warnings:
          self._log_panel.log(f"  WARN: {warning}")

      _ask_duplicate_action(self, monster_name: str) -> str:
        box = QMessageBox(self)
        box.setWindowTitle("Duplicate Monster")
        box.setText(f'"{monster_name}" already exists in the library.')
        box.setInformativeText("What would you like to do?")
        keep_btn = box.addButton("Keep Both", QMessageBox.ButtonRole.AcceptRole)
        replace_btn = box.addButton("Replace", QMessageBox.ButtonRole.DestructiveRole)
        skip_btn = box.addButton("Skip", QMessageBox.ButtonRole.RejectRole)
        box.exec()
        if box.clickedButton() == keep_btn: return 'keep'
        if box.clickedButton() == replace_btn: return 'replace'
        return 'skip'

      _refresh_model(self):
        self._model.reset_monsters(self._library.all())

      _refresh_type_combo(self):
        """Repopulate type dropdown with current library's creature types."""
        current = self._type_combo.currentText()
        self._type_combo.blockSignals(True)
        self._type_combo.clear()
        self._type_combo.addItem("All Types")
        for t in self._library.creature_types():
          self._type_combo.addItem(t)
        # Restore previous selection if it still exists
        idx = self._type_combo.findText(current)
        if idx >= 0:
          self._type_combo.setCurrentIndex(idx)
        self._type_combo.blockSignals(False)

    Also add set_complete_only(bool) to MonsterFilterProxyModel in src/ui/monster_filter.py if
    "Complete only" filter is needed. Add self._complete_only: bool = False field and check:
      if self._complete_only and monster.incomplete: return False
    This is a small additive change to monster_filter.py from Plan 03.
  </action>
  <verify>
    python -c "from src.ui.library_tab import MonsterLibraryTab; from src.library.service import MonsterLibrary; print('OK')"
    Manual smoke test (requires display):
      python -c "
      import sys
      from PySide6.QtWidgets import QApplication
      from src.library.service import MonsterLibrary
      from src.ui.library_tab import MonsterLibraryTab
      app = QApplication(sys.argv)
      lib = MonsterLibrary()
      tab = MonsterLibraryTab(lib)
      tab.resize(900, 600)
      tab.show()
      sys.exit(app.exec())
      "
    Verify: window opens with splitter layout, Import button with menu, search bar, type combo,
    incomplete filter combo, table with Name/CR/Type/badge columns, detail panel, log panel.
  </verify>
  <done>
    - MonsterLibraryTab renders with toolbar, table, detail panel, log panel in splitter layout
    - Import > File(s) opens multi-file dialog; imported monsters appear in table
    - Import > Folder scans directory; results shown in scrollable log panel
    - Duplicate name triggers Keep Both / Replace / Skip dialog
    - Search bar filters table to matching monsters; type combo narrows by creature type
    - Incomplete filter shows only incomplete monsters
    - Column header click sorts by name (string) or CR (numeric float)
    - Clicking table row shows full statblock in detail panel
    - Parsed actions show Roll button; unparsed actions show raw text only
    - Lore section collapsed by default; expandable via checkbox toggle
    - Tags editable in detail panel; edit updates Monster.tags in library
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human Verification — Full Library Tab End-to-End</name>
  <files>N/A — verification only, no files modified</files>
  <action>Human verification of the completed library tab. Follow the how-to-verify steps below to confirm all behaviors work correctly.</action>
  <verify>See how-to-verify steps below — perform all 10 checks and confirm expected behavior at each step.</verify>
  <done>All 10 verification steps pass: import works, duplicate dialog appears, search/filter/sort all function, detail panel shows correct statblock, lore collapsed by default, tags editable.</done>
  <what-built>
    Complete Monster Library tab: 3-format parser (5etools + Homebrewery + plain), MonsterLibrary
    service, MonsterTableModel + MonsterFilterProxyModel, MonsterLibraryTab with import toolbar,
    scrollable log panel, search/filter/sort controls, and MonsterDetailPanel with collapsible lore.
  </what-built>
  <how-to-verify>
    1. Launch the library tab:
       python -c "
       import sys
       from PySide6.QtWidgets import QApplication, QMainWindow
       from src.library.service import MonsterLibrary
       from src.ui.library_tab import MonsterLibraryTab
       app = QApplication(sys.argv)
       lib = MonsterLibrary()
       tab = MonsterLibraryTab(lib)
       win = QMainWindow()
       win.setCentralWidget(tab)
       win.resize(1100, 700)
       win.setWindowTitle('RollinRollin - Library Test')
       win.show()
       sys.exit(app.exec())
       "

    2. Import a file: Click "Import…" > "File(s)…" → select .claude/bestiary(1).md
       EXPECTED: Monster(s) appear in the table with Name, CR, Type columns. Log panel shows
       "--- bestiary(1).md ---" with Imported/Incomplete/Failures counts.

    3. Click a monster row.
       EXPECTED: Detail panel shows correct name, AC, HP, CR, type, ability scores (all 6),
       actions. Actions with attack syntax have "Roll" button (grayed out).
       Actions without attack syntax show raw text, no button.

    4. Type "mед" or any partial monster name in the search bar.
       EXPECTED: Table filters instantly to matching rows only.

    5. Select a type from the Type dropdown.
       EXPECTED: Table filters to only that creature type; search filter still applies.

    6. Click the "Name" column header.
       EXPECTED: Table sorts A→Z; click again sorts Z→A.
       Click "CR" column header.
       EXPECTED: Table sorts 1/8 < 1/4 < 1/2 < 1 < 2 < ... < 30 (numeric, not alphabetical).

    7. Select "Incomplete only" from the incomplete dropdown.
       EXPECTED: Only monsters with "!" badge visible; others hidden.

    8. If lore text is present in a monster, click the monster and look at the detail panel bottom.
       EXPECTED: "Lore & Description" group box is collapsed (checkbox unchecked); clicking it expands.

    9. Try importing a folder (Import > Folder).
       EXPECTED: All .md files in the folder are processed; log panel shows entry per file.

    10. Import the same file again.
        EXPECTED: Duplicate dialog appears with "Keep Both", "Replace", "Skip" options.
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors work as described, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
python -m pytest src/tests/ -v — all tests pass (Phase 1 + all Phase 2 tests)
python -c "from src.ui.library_tab import MonsterLibraryTab; from src.ui.monster_detail import MonsterDetailPanel; from src.ui.import_log import ImportLogPanel; print('All imports OK')"
</verification>

<success_criteria>
- MonsterLibraryTab renders and all UI controls are functional
- Import File(s) and Import Folder both work; results in log panel (not modal)
- Duplicate monster triggers per-name dialog (Keep Both / Replace / Skip)
- Search bar, type filter, incomplete filter all work with AND logic
- Column header click sorts; CR column sorts numerically
- Clicking monster row shows full statblock in detail panel
- Parsed actions have Roll button (disabled); unparsed actions show raw text only
- Lore collapsed by default; expandable
- Tags editable in detail panel
- All 13 requirements (IMPORT-01 through LIB-06) satisfied
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-monster-import-and-library/02-04-SUMMARY.md`
</output>
