---
phase: 08-domain-expansion-and-persistence-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/ui/app.py
  - src/ui/settings_tab.py
autonomous: true
requirements:
  - PERSIST-03

must_haves:
  truths:
    - "App auto-saves all persistence data every 30 seconds via QTimer"
    - "App saves all persistence data on window close via closeEvent"
    - "Status bar shows 'Saved' message briefly after each auto-save"
    - "Settings tab displays per-category flush buttons with entry counts"
    - "Clicking a flush button shows a confirmation dialog; confirming flushes that category"
    - "'Clear All Data' button flushes all four categories after confirmation"
    - "After flushing, the entry counts refresh immediately to show 0"
    - "PersistenceService loads data on app start (MainWindow.__init__)"
  artifacts:
    - path: "src/ui/app.py"
      provides: "Auto-save timer, closeEvent, statusBar, PersistenceService wiring"
      contains: "closeEvent"
    - path: "src/ui/settings_tab.py"
      provides: "Data Management flush section with per-category buttons and Clear All"
      contains: "flush_requested"
  key_links:
    - from: "src/ui/app.py"
      to: "src/persistence/service.py"
      via: "PersistenceService instantiated in MainWindow.__init__"
      pattern: "PersistenceService"
    - from: "src/ui/settings_tab.py"
      to: "src/ui/app.py"
      via: "flush_requested Signal(str) connected in MainWindow"
      pattern: "flush_requested"
    - from: "src/ui/app.py"
      to: "QTimer auto-save"
      via: "QTimer.timeout connected to _autosave method"
      pattern: "_autosave_timer"
---

<objective>
Wire PersistenceService into the application lifecycle: auto-save timer, save-on-close, status bar feedback, and a "Data Management" flush section in the Settings tab.

Purpose: This completes the persistence story — data now saves automatically during use, on close, and can be selectively or fully flushed by the DM from Settings. The DM never has to manually save; the app handles it transparently.
Output: Modified app.py with auto-save lifecycle and modified settings_tab.py with flush UI.
</objective>

<execution_context>
@C:/Users/farka/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/farka/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-domain-expansion-and-persistence-foundation/08-RESEARCH.md
@.planning/phases/08-domain-expansion-and-persistence-foundation/08-01-SUMMARY.md
@src/ui/app.py
@src/ui/settings_tab.py
@src/persistence/service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Data Management flush section to Settings tab</name>
  <files>src/ui/settings_tab.py</files>
  <action>
Add two new signals to SettingsTab class:
- `flush_requested = Signal(str)` — emits category key (e.g. "loaded_monsters")
- `clear_all_requested = Signal()`

Add a new method `_build_data_flush_group(self) -> QGroupBox` that creates a "Data Management" group:

For each of the 4 categories (`loaded_monsters`/"Loaded Monsters", `encounters`/"Encounters", `modified_monsters`/"Modified Monsters", `macros`/"Macros"):
- Create a QHBoxLayout row containing:
  - A QLabel showing `"{DisplayName}: -- entries"` (placeholder count, updated by refresh_counts)
  - A QPushButton labeled `"Flush {DisplayName}"`
  - Wire button click to `_on_flush(category_key, display_name)`
- Store labels in `self._flush_labels: dict[str, QLabel]` and buttons in `self._flush_btns: dict[str, QPushButton]` for later access.

Add a "Clear All Data" QPushButton at the bottom of the group. Wire its click to `_on_clear_all()`.

`_on_flush(self, category: str, display_name: str)` method:
- Show `QMessageBox.question()` with text: `f"Flush all {display_name} data? This cannot be undone."`
- Buttons: Yes | Cancel
- If Yes: emit `self.flush_requested.emit(category)`

`_on_clear_all(self)` method:
- Show `QMessageBox.question()` with text: `"Clear ALL persistent data? This cannot be undone."`
- Buttons: Yes | Cancel
- If Yes: emit `self.clear_all_requested.emit()`

Add a public method `refresh_counts(self, counts: dict[str, int])`:
- For each category key in `self._flush_labels`, update the label text to `f"{DisplayName}: {count} {'entry' if count == 1 else 'entries'}"`
- Derive DisplayName from key: `key.replace("_", " ").title()`

Wire the new group into `_setup_ui()`:
- Add `self._build_data_flush_group()` to the outer layout BEFORE the Save button (insert between the grid_widget and the Save button).

Add QMessageBox to the widget imports if not already imported (it is not currently imported in settings_tab.py — add it).
  </action>
  <verify>
Run `python -c "from src.ui.settings_tab import SettingsTab; print('flush_requested' in dir(SettingsTab))"` — must print True.
Run the app (`python src/main.py`), navigate to Settings tab, verify the "Data Management" group appears with four flush buttons and a "Clear All Data" button.
  </verify>
  <done>
Settings tab shows a "Data Management" section with per-category flush buttons (showing entry counts), a "Clear All Data" button, and confirmation dialogs on all flush actions. flush_requested and clear_all_requested signals are defined and emittable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire PersistenceService lifecycle into MainWindow</name>
  <files>src/ui/app.py</files>
  <action>
Add imports to app.py:
```python
from PySide6.QtCore import QTimer
from src.persistence.service import PersistenceService
from src.workspace.setup import resolve_workspace_root
```

**Workspace root update:**
Replace the hardcoded `Path.home() / "RollinRollin"` with `resolve_workspace_root()`:
```python
self._workspace_manager = WorkspaceManager(resolve_workspace_root())
```

**PersistenceService instantiation** (in `__init__`, after workspace_manager.initialize()):
```python
self._persistence = PersistenceService(self._workspace_manager.root)
self._load_persisted_data()
```

**Auto-save timer** (in `__init__`, after tab construction):
```python
self._autosave_timer = QTimer(self)
self._autosave_timer.setInterval(30_000)  # 30 seconds
self._autosave_timer.timeout.connect(self._autosave)
self._autosave_timer.start()
```

**Status bar** (in `__init__`):
```python
self.statusBar().showMessage("Ready")
```

**New methods:**

`_autosave(self)`:
- Call `self._save_persisted_data()`
- Show status bar message: `self.statusBar().showMessage("Saved", 2000)` (disappears after 2s)

`closeEvent(self, event)`:
- Call existing unsaved-settings check first: if Settings tab is dirty, prompt save/discard (same logic as _on_tab_changed for settings tab index)
- Call `self._save_persisted_data()`
- `event.accept()`

`_load_persisted_data(self)`:
- For Phase 8, this is a skeleton. Load each category from PersistenceService and store in instance variables:
  - `self._persisted_monsters = self._persistence.load_loaded_monsters()`
  - `self._persisted_encounters = self._persistence.load_encounters()`
  - `self._persisted_modifications = self._persistence.load_modified_monsters()`
  - `self._persisted_macros = self._persistence.load_macros()`
- Later phases (9, 10, 11) will wire these to the actual Library, EncountersTab, etc.

`_save_persisted_data(self)`:
- Save each category back:
  - `self._persistence.save_loaded_monsters(self._persisted_monsters)`
  - `self._persistence.save_encounters(self._persisted_encounters)`
  - `self._persistence.save_modified_monsters(self._persisted_modifications)`
  - `self._persistence.save_macros(self._persisted_macros)`

**Flush signal wiring** (in `__init__`, after settings_tab construction):
```python
self._settings_tab.flush_requested.connect(self._on_flush_category)
self._settings_tab.clear_all_requested.connect(self._on_clear_all)
```

`_on_flush_category(self, category: str)`:
- `self._persistence.flush(category)`
- Reset the corresponding `self._persisted_*` variable to empty ([] or {})
- Call `self._refresh_flush_counts()`

`_on_clear_all(self)`:
- `self._persistence.flush_all()`
- Reset all `self._persisted_*` to empty
- Call `self._refresh_flush_counts()`

`_refresh_flush_counts(self)`:
- Build `counts = {cat: self._persistence.count(cat) for cat in self._persistence.categories()}`
- `self._settings_tab.refresh_counts(counts)`

**Tab change hook update:**
In the existing `_on_tab_changed` method, add a call to `self._refresh_flush_counts()` when switching TO the settings tab:
```python
if new_index == self._settings_tab_index:
    self._refresh_flush_counts()
```

Remove the `from pathlib import Path` import if it is no longer needed after the workspace root change. Keep it if still used.
  </action>
  <verify>
Run `python src/main.py`:
1. App starts without errors and status bar shows "Ready"
2. After 30 seconds, status bar briefly shows "Saved"
3. Navigate to Settings tab — flush buttons show "0 entries" for all categories
4. Close the app — no errors on close

Run `python -m pytest` — full test suite passes with zero regressions.
  </verify>
  <done>
MainWindow creates PersistenceService, loads data on startup, auto-saves every 30s with status bar feedback, saves on close via closeEvent, and wires flush signals from SettingsTab to PersistenceService. Flush counts refresh when switching to Settings tab and after any flush operation.
  </done>
</task>

</tasks>

<verification>
1. App starts: status bar shows "Ready", no errors
2. Wait 30s: status bar briefly shows "Saved"
3. Settings tab: "Data Management" section visible with 4 flush buttons and "Clear All Data" button
4. Flush buttons show entry counts (all 0 for fresh install)
5. Clicking any flush button shows confirmation dialog
6. Close app: no errors, no data loss (data files exist in workspace root)
7. `python -m pytest` — full test suite passes
</verification>

<success_criteria>
- Auto-save fires every 30 seconds and shows "Saved" in status bar
- closeEvent saves all data before accepting close
- PersistenceService is loaded on startup and wired throughout the app lifecycle
- Settings tab shows per-category flush buttons with live entry counts
- Flush confirmation dialogs prevent accidental data loss
- "Clear All Data" flushes all categories after confirmation
- Entry counts refresh after flush and on tab switch
- resolve_workspace_root() is used instead of hardcoded path
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-domain-expansion-and-persistence-foundation/08-03-SUMMARY.md`
</output>
