---
phase: 15-editor-parser-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/ui/monster_editor.py
autonomous: true
requirements:
  - PARSE-01
  - PARSE-02
  - PARSE-03
  - PARSE-04
  - PARSE-10
  - PARSE-11

must_haves:
  truths:
    - "The editor has a single 'Core Stats' collapsible section containing ability scores, CR, HP, Speed, and Skills — no longer separate collapsible sections for each"
    - "The editor section order top-to-bottom is: Name > Core Stats > Saving Throws > Traits > Actions > Equipment > Buffs"
    - "Traits appear as a compact name list in a collapsible 'Traits' section; clicking a trait opens a QDialog modal with editable title and multiline description"
    - "Traits with detected rollable dice show a small 'rollable' indicator next to their name in the list"
    - "Each action row has a column header row above it with labels like 'Name', 'To-Hit', 'Damage', 'Type', etc."
    - "Each action row has an expandable after-attack-text field (toggle button reveals multiline QTextEdit)"
    - "Equipment section is positioned at the bottom of the editor, right above Buffs"
  artifacts:
    - path: "src/ui/monster_editor.py"
      provides: "_build_core_stats_section(), _build_traits_section(), action header row, after-text toggle, section reorder"
      contains: "_build_core_stats_section"
  key_links:
    - from: "src/ui/monster_editor.py"
      to: "src/domain/models.py"
      via: "Trait import for trait list management"
      pattern: "from.*models.*import.*Trait"
    - from: "src/ui/monster_editor.py (_build_traits_section)"
      to: "src/ui/monster_editor.py (_rebuild_preview)"
      via: "Trait edit modal triggers preview rebuild on close"
      pattern: "_rebuild_preview"
---

<objective>
Restructure the Monster Editor layout — merge ability scores, CR, HP, Speed, and Skills into a single "Core Stats" section, add a Traits section with click-to-edit modal, add column header labels to action rows, add expandable after-attack-text fields to each action, and reposition Equipment to the bottom above Buffs.

Purpose: The editor currently has too many separate collapsible sections requiring excessive scrolling. This plan compacts the layout per user decision and adds the Traits section UI that displays traits separated by Plan 01's parser changes.

Output: A restructured MonsterEditorDialog with Core Stats merge, Traits section, labeled action columns, after-text editing, and Equipment at bottom.
</objective>

<execution_context>
@C:/Users/farka/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/farka/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-editor-parser-overhaul/15-RESEARCH.md
@.planning/phases/15-editor-parser-overhaul/15-CONTEXT.md
@.planning/phases/15-editor-parser-overhaul/15-01-SUMMARY.md
@.planning/phases/09-monster-editor-and-equipment-presets/09-03-SUMMARY.md

@src/ui/monster_editor.py
@src/domain/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge Core Stats section and reorder editor sections</name>
  <files>src/ui/monster_editor.py</files>
  <action>
Restructure `_setup_ui()` (or equivalent section-building method) in MonsterEditorDialog:

**1. Create `_build_core_stats_section()` that merges:**
- Ability scores grid (6 QSpinBox widgets in a row — the existing `_build_ability_scores_section` content)
- A CR | HP | Speed horizontal row below ability scores:
  - CR: QComboBox (existing from `_build_cr_section`)
  - HP: flat max QSpinBox + hit dice formula QLineEdit (existing from `_build_hp_section`)
  - Speed: new QLineEdit with placeholder "e.g. 30 ft., fly 60 ft." — initialize from `_working_copy.speed`
  - Layout these 3 as a compact horizontal row with labels
- Skills: existing skill rows + add-skill combo (from `_build_skills_section`) at the bottom of the Core Stats section
- Wrap the entire content in a single `CollapsibleSection("Core Stats", content, expanded=True)`

**2. Store `self._speed_edit` QLineEdit** so it can be read back when rebuilding preview and syncing to `_working_copy`. Connect `textChanged` signal to update `_working_copy.speed` and call `_rebuild_preview()`.

**3. Update `_populate_form()`** to also populate the speed QLineEdit from `_working_copy.speed` during undo restore.

**4. Reorder sections in `_setup_ui()` top-to-bottom:**
- Name row (toolbar — already first)
- Core Stats (new merged section — `_build_core_stats_section()`)
- Saving Throws (`_build_saving_throws_section()` — stays separate, per CONTEXT.md which did not include it in the merge)
- Traits (new — Task 2 adds this)
- Actions (`_build_actions_section()`)
- Equipment (`_build_equipment_section()` — moved from current position to here)
- Buffs (`_build_buffs_section()` — stays last)

**5. Remove old separate section builders** that are now merged: `_build_ability_scores_section()`, `_build_cr_section()`, `_build_hp_section()`, `_build_skills_section()`. Their internal logic moves into `_build_core_stats_section()`. Keep their internal helper methods (like the ability spinbox creation) as private methods called by `_build_core_stats_section()`.

**Important:** Preserve all existing signal connections (ability score valueChanged, CR currentIndexChanged, HP valueChanged, skill toggle signals). Only the section wrapping and ordering changes — the internal widget behavior stays the same. Preserve `_recalculating` guard pattern.
  </action>
  <verify>Open the Monster Editor on any monster. Verify:
1. A single "Core Stats" collapsible section exists containing ability scores, CR/HP/Speed row, and Skills
2. Saving Throws is its own separate section below Core Stats
3. Equipment section is near the bottom, above Buffs
4. All existing functionality works: ability score changes cascade, CR changes cascade, HP edits work, skills add/remove works
5. Speed field shows the monster's speed (or empty for old data)
6. Undo/redo restores speed field correctly
7. `python -m pytest` — all tests pass
  </verify>
  <done>Core Stats section merges ability scores, CR, HP, Speed, Skills into one collapsible section. Editor section order is Name > Core Stats > Saving Throws > Traits > Actions > Equipment > Buffs. All existing functionality preserved. Speed field editable and wired to preview.</done>
</task>

<task type="auto">
  <name>Task 2: Add Traits section UI, action column headers, and after-attack-text editing</name>
  <files>src/ui/monster_editor.py</files>
  <action>
**1. Build `_build_traits_section()` — a new collapsible section for traits:**

- Create a `CollapsibleSection("Traits", content, expanded=False)` positioned between Saving Throws and Actions
- Inside: a QVBoxLayout containing a compact list of trait widgets
- Maintain `self._trait_items: list[Trait]` as editor-local state, initialized from `_working_copy.traits` (mirror the existing buff pattern from `_buff_items`)
- Each trait row is a horizontal widget:
  - QLabel with the trait name
  - If trait has `rollable_dice` (non-empty list): add a small QLabel tag like "[rollable]" or a dice icon indicator next to the name (per user decision: "Traits with detected rollable dice show a small dice icon or 'rollable' tag")
  - An "Edit" QPushButton that opens a `TraitEditDialog`
  - A "Remove" QPushButton (small "x" or minus icon) that removes the trait from `_trait_items` and rebuilds
- An "Add Trait" QPushButton at the bottom that adds a new Trait(name="New Trait", description="") and opens the edit modal immediately
- Method: `_rebuild_trait_rows()` — clears and rebuilds trait row widgets (same pattern as `_rebuild_action_rows()` and `_rebuild_buff_rows()`)

**2. Create `TraitEditDialog(QDialog)` — a modal for editing a single trait:**

- Title: "Edit Trait"
- Fields:
  - "Name" QLineEdit for trait name
  - "Description" QTextEdit (multiline) for trait description text
- OK / Cancel buttons (QDialogButtonBox with Accept/Reject)
- On Accept: update the Trait object's name and description, return QDialog.Accepted
- After modal closes with Accepted: re-detect rollable dice by calling `detect_dice_in_text(trait.description)` and `detect_recharge(trait.name)` from _shared_patterns, update trait.rollable_dice and trait.recharge_range, then rebuild trait rows + rebuild preview
- Sync `_trait_items` back to `_working_copy.traits` before every `_push_undo()` call (mirror buff pattern)
- In `_populate_form()` (undo restore): repopulate `_trait_items` from `_working_copy.traits` and call `_rebuild_trait_rows()`

**3. Add action column header row:**

- In `_rebuild_action_rows()`, before the loop that creates action row widgets, insert a fixed header row QWidget with QHBoxLayout containing QLabel columns: "Name", "To-Hit", "Damage Dice", "Damage Type", "Bonus" (or whatever columns match the existing action row field layout)
- Use a slightly different style (bold text or subtle background) to distinguish from data rows — match a table-header feel per user decision
- Store header widget so it's not duplicated on rebuild (clear it with other rows in the rebuild method)

**4. Add after-attack-text toggle per action row:**

- In each action row created by `_rebuild_action_rows()`, add a small toggle QPushButton (e.g. "..." or "More") at the end of the row
- When clicked, show/hide a QTextEdit below the action row that contains `action.after_text`
- The after-text field is editable — text changes update `self._working_copy.actions[idx].after_text` and trigger `_rebuild_preview()`
- Per user decision: "After-attack-text field is expandable on demand — a toggle/button reveals the multiline text area, keeps default action view clean"
- The toggle starts hidden (collapsed) by default
- Connect QTextEdit textChanged to update the action's after_text on the working copy

**Important behavioral rules:**
- After-text only displayed in roll output on hits, not misses (this is output behavior — Plan 03 handles it)
- [[XdY]] average notation rendering is handled by Plan 03 in the detail panel
  </action>
  <verify>Open the Monster Editor on a freshly imported monster (with traits populated by Plan 01 parser):
1. Traits section exists between Saving Throws and Actions
2. Traits show as a compact name list with "rollable" tags where applicable
3. Clicking "Edit" on a trait opens a modal with name and description fields
4. Editing trait name/description and clicking OK updates the trait and rebuilds preview
5. "Add Trait" creates a new trait and opens the edit modal
6. "Remove" removes the trait from the list
7. Action rows have a column header row above them with labels
8. Each action row has a "..." toggle button that reveals after-attack-text QTextEdit
9. Editing after-text updates the preview
10. Undo restores traits and after-text state correctly
11. `python -m pytest` — all tests pass
  </verify>
  <done>Traits section displays traits as compact name list with edit modal, rollable indicator, add/remove. Action rows have column header labels. Each action has expandable after-attack-text field. All state syncs with undo stack and preview.</done>
</task>

</tasks>

<verification>
1. `python -m pytest` — all tests pass
2. Editor opens without errors on any monster
3. Core Stats section contains ability scores + CR/HP/Speed + Skills in one collapsible section
4. Section order: Name > Core Stats > Saving Throws > Traits > Actions > Equipment > Buffs
5. Traits section shows trait names with rollable indicators; edit modal works
6. Action header labels visible above action rows
7. After-text toggles expand to show editable text
8. Speed field editable and reflected in preview
9. Undo/redo covers all new fields (traits, speed, after-text)
</verification>

<success_criteria>
- Single "Core Stats" collapsible section replaces 4 separate sections
- Editor section order matches user decision: Name > Core Stats > Saving Throws > Traits > Actions > Equipment > Buffs
- Traits section with compact list, rollable indicators, edit modal, add/remove
- Action column header labels visible
- After-attack-text expandable fields per action
- Speed field in Core Stats, wired to working copy and preview
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/15-editor-parser-overhaul/15-02-SUMMARY.md`
</output>
